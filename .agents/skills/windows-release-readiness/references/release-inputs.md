# Windows Release Inputs

Before publication, verify a semantic application version and app ID, real GitHub owner/repository values without `YOUR_*` placeholders, intentional production update settings, configured CI signing secrets, a clean Node-version-compatible install, successful release validation, build, and artifact inspection.

The release workflow may build artifacts after a tag is pushed, but agents must not push tags or publish releases without explicit user approval.
